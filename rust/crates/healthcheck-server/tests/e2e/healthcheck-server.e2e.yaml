server:
  proxy_socket: "/var/run/seesaw/healthcheck.sock"

batching:
  delay: 100ms
  max_size: 10

manager:
  monitor_interval: 500ms

metrics:
  enabled: true
  listen_addr: "0.0.0.0:9090"

telemetry:
  enabled: false

logging:
  level: "debug"
  format: "text"
