server {
    listen 80;
    server_name localhost;

    location /health {
        root /usr/share/nginx/html;
        try_files /healthy.txt =503;
        default_type text/plain;
    }

    location / {
        return 200 'target-service';
        default_type text/plain;
    }
}
